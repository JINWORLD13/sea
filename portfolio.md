# SEATRACE — 실시간 AIS 선박 관제 시스템

> 신입 프론트엔드 개발자 지원용 개인 프로젝트 (1인 개발)
> 전 세계 실시간 AIS 스트림을 받아 선박을 추적하고 충돌 위험을 계산하는 웹 관제 화면
> English version: [portfolio.en.md](portfolio.en.md)

---

## 한 줄 요약

**초당 수백 건씩 들어오는 실시간 AIS WebSocket 스트림을, 브라우저가 버틸 수 있는 속도로 낮추는 것**이 이 프로젝트의 핵심 과제였음. 서버·스트림·스토어·렌더 4개 계층에 각각 다른 방식의 부하 제어를 넣어 해결했음.

---

## 프로젝트 개요

|  |  |
|---|---|
| **프로젝트명** | SEATRACE — Real-time AIS Vessel Tracking |
| **기간 / 인원** | 2026.02 ~ 2026.03 · 1인 (설계 · 프론트 · 프록시 서버 · 해사 알고리즘) |
| **데이터 소스** | 외부 API [AISStream.io](https://aisstream.io) — 전 세계 실시간 AIS WebSocket (**모의 데이터 아님**) |
| **라이브 데모** | [vts-tau-navy.vercel.app](https://vts-tau-navy.vercel.app) — 프록시가 무료 서버라 첫 접속은 느릴 수 있음 |
| **GitHub** | [github.com/JINWORLD13/sea](https://github.com/JINWORLD13/sea) |
| **규모** | TypeScript/TSX 약 9,000줄 + Node 프록시 약 1,100줄 |
| **주요 화면** | Dashboard · Live Map · Fleet Status · Analytics · Settings |

### 왜 만들었나

선박 운항의 디지털화에 관심이 있었고, **"진짜 실시간 데이터"를 다루면 어떤 문제가 생기는지** 직접 겪어보고 싶었음. 목업 데이터로 만든 대시보드는 예쁘게 나오지만, 실제 스트림을 붙이는 순간 나타나는 문제 — 초당 수백 건의 유입, 불규칙한 결측, 끊기는 소켓, 수백 개 마커가 만드는 프레임 드랍 — 은 겪어볼 수 없기 때문임.

---

## 기술 스택

| 영역 | 사용 기술 |
|---|---|
| **Frontend** | React 19, TypeScript 5.9, Vite 7 |
| **상태 관리** | Zustand 5 (셀렉터 기반 구독) |
| **지도** | Leaflet 1.9, React-Leaflet 5 |
| **3D** | Three.js, @react-three/fiber, @react-three/drei |
| **백엔드(프록시)** | Node.js, Express 5, `ws` |
| **스타일링** | Tailwind CSS 3, Lucide React |
| **국제화** | i18next (한국어 · 영어 · 일본어) |
| **테스트** | Vitest 42개 (해상 수학 · 위험 등급 판정 순수 함수 + 스토어 위험 판정 패스) |

---

## 🔑 핵심: 4계층 부하 제어 파이프라인

이 프로젝트에서 **가장 자신 있게 설명할 수 있는 부분**임. AIS 원본 스트림과 화면 사이에 4개의 감쇠 단계를 두었고, 각 단계가 서로 다른 종류의 부하를 담당함.

```
AISStream 원본  ─── 혼잡 해역 기준 초당 수백 msg ──┐
                                                   │
 ┌─────────────────────────────────────────────────▼──────────────────────┐
 │ ① 프록시 서버 (server/proxy.js)                                        │
 │   · 구독 박스 면적 ≤ 0.25 제곱도 강제 검증 → 과도한 구독 차단           │
 │   · 클라이언트당 초당 180 msg / 추적 600 MMSI 상한 (초과분 drop 카운트) │
 │   · 서버 LRU 캐시 5,000척 (TTL 10분) → 신규 접속 시 즉시 스냅샷 응답    │
 │   · 스냅샷을 100척 청크로 분할 전송 → 단일 거대 프레임 방지             │
 └─────────────────────────────────────────────────┬──────────────────────┘
                                                   │  1클라이언트 ≤ 180 msg/s
 ┌─────────────────────────────────────────────────▼──────────────────────┐
 │ ② 스트림 계층 (src/store/aisStream.ts)                                  │
 │   · MMSI를 key로 하는 Map에 병합 누적 → 같은 배의 연속 보고를 1건으로   │
 │   · 1초 간격 배치 flush (접속 직후 첫 배치만 즉시 → 초기 로딩 체감 개선) │
 │   · 구독 박스 밖 좌표는 스토어 진입 전에 폐기                           │
 └─────────────────────────────────────────────────┬──────────────────────┘
                                                   │  최대 1 set() / 초
 ┌─────────────────────────────────────────────────▼──────────────────────┐
 │ ③ 스토어 계층 (src/store/useShipStore.ts)                               │
 │   · 추적 선박 500척 상한 / 20분 미수신 자동 정리                        │
 │   · CPA·지오펜스를 단일 패스 + copy-on-write로 계산 (아래 상세)          │
 └─────────────────────────────────────────────────┬──────────────────────┘
                                                   │
 ┌─────────────────────────────────────────────────▼──────────────────────┐
 │ ④ 렌더 계층 (src/hooks/useShipSnapshot.ts + components/map/)            │
 │   · 화면별 800~1,200ms 스냅샷 스로틀 + startTransition (입력 우선)      │
 │   · 화면(viewport) 밖 선박 제외 → 렌더 250개 상한 (중심 근접 우선)      │
 │   · 줌 12 미만은 64px 픽셀 격자 클러스터링                              │
 │   · 마커 이동은 공유 rAF 티커로 보간 → React 리렌더 0회                 │
 └─────────────────────────────────────────────────┬──────────────────────┘
                                                   ▼
                                               화면 (부드러운 프레임 유지)
```

---

## 기술적 도전과 해결

### 1. API 키를 브라우저에 노출하지 않으면서 실시간 스트림 받기

**문제** — AISStream은 WebSocket 연결 직후 API 키를 담은 구독 메시지를 요구함. 프론트에서 직접 연결하면 `VITE_` 환경변수가 번들에 그대로 박혀 **누구나 개발자 도구로 키를 꺼낼 수 있음.**

**해결** — Node/Express + `ws` 기반 프록시 서버를 직접 작성했음 ([server/proxy.js](server/proxy.js)).

- API 키는 서버 프로세스에만 존재하고, 브라우저는 프록시하고만 통신함
- 프론트↔프록시 사이에 `t` 필드로 판별하는 **자체 와이어 프로토콜**을 정의했음 (`pos` / `static` / `snapshot` / `snapshotEnd` / `error`)
- AIS 센티널 값(속도 102.3 = 미상, 침로 511 = 미상 등)을 **서버에서 `null`로 정규화**해, 프론트가 도메인 예외를 몰라도 되게 했음
- 여러 클라이언트의 구독 박스를 합집합으로 묶어 상류 재구독은 2초 디바운스 (최대 8박스)
- **클라이언트 입력은 신뢰하지 않음** — 객체가 아닌 페이로드·범위 밖 좌표는 검증 단계에서 거부하고, 프레임 크기 64KB 상한(`ws` 기본값 100MB 대체)·구독 플러드는 300ms 창마다 최신 1건으로 합침·30초 ping/pong 하트비트·`ALLOWED_ORIGINS` Origin 허용목록으로 공개 엔드포인트를 방어함. 마지막 안전망으로 `uncaughtException` 핸들러를 두어, 예외 하나가 전체 클라이언트 연결과 캐시를 잃게 만들지 않음

**와이어 프로토콜 스펙**

| `t` | 페이로드 | 발생 시점 · 빈도 |
| --- | --- | --- |
| `pos` | `mmsi, lat, lng, sog, cog, hdg, nav, name, kind, ts` | 위치 보고(Msg 1/2/3/18/19/21)마다 · 고빈도 |
| `static` | `mmsi, name, imo, callsign, type, dest, eta, length, width, draught, ts` | 정적 보고(Msg 5/24)마다 · 약 6분 주기 |
| `snapshot` | `ships: [...]` — pos + static을 평탄화해 병합한 배열 | 접속·재구독 직후 1회 · 100척 청크 |
| `snapshotEnd` | `total` | 스냅샷 청크 종료 신호 |
| `error` | `error, message` | 구독 검증 실패 등 (코드로 구분) |

클라이언트 → 프록시 방향은 구독 메시지 한 종류뿐임: `{ "BoundingBoxes": [[[minLat, minLng], [maxLat, maxLng]]] }`

`pos`와 `static`을 나눈 기준은 **변경 빈도**임. 위경도·속도는 초 단위로 바뀌지만 선명·IMO·치수는 몇 시간에 한 번 바뀌므로, 한 덩어리로 보내면 매초 안 변하는 데이터를 재전송하게 됨. 프론트는 MMSI를 key로 하는 `Map`에서 둘을 병합해 하나의 `Ship`으로 만듦.

**결과** — 키 노출 0. 동시에 프론트 코드에서 AIS 프로토콜 파싱 로직이 완전히 사라져, 클라이언트는 정제된 JSON만 다루면 됨.

---

### 2. 지도를 움직일 때마다 구독 영역을 바꾸기

**문제** — 전 세계를 한 번에 구독하면 초당 수천 건이 들어와 브라우저가 멈춤. 그렇다고 고정된 해역만 구독하면 사용자가 지도를 옮겼을 때 빈 화면이 됨.

**해결** — 뷰포트 기반 동적 재구독을 구현했음 ([aisStream.ts:593](src/store/aisStream.ts:593)).

- 지도 `moveend` / `zoomend` 후 **400ms 디바운스** → 드래그 중 수십 번 재구독하는 것 방지
- 화면 박스에 **15% 패딩**을 더해 구독 → 패닝 시 가장자리가 비지 않음
- 패딩 후 면적이 프록시 제한(0.25°²)을 넘으면 **현재 해역 박스로 폴백**하고 클라이언트에서 클러스터링
- **소켓을 끊지 않고** 구독 메시지만 다시 보냄 → 재연결 지연 없음
- 새 박스 밖 선박은 즉시 스토어에서 정리 (선택된 선박은 예외로 계속 추적)

---

### 3. React StrictMode에서 소켓이 유령처럼 남는 문제

**문제** — 개발 모드에서 React가 이펙트를 두 번 실행하면서, 정리된 줄 알았던 이전 소켓의 `onmessage` / 재접속 타이머가 살아남아 데이터가 중복 반영되고 재접속이 폭주했음.

**해결** — **세대(generation) 카운터** 패턴을 도입했음 ([aisStream.ts:36](src/store/aisStream.ts:36)).

```ts
let streamGeneration = 0;

export const startAisStream = (bounds: RegionBounds): void => {
  streamGeneration += 1;               // 먼저 세대를 올리고
  const generation = streamGeneration; // 이 시점의 세대를 캡처
  // ...
};

// 모든 콜백은 자기 세대가 최신인지 먼저 확인한다
socket.onmessage = (event) => {
  if (generation !== streamGeneration || activeSocket !== socket) return;
  // ...
};
```

이전 세대에 속한 모든 콜백이 **자동으로 no-op이 됨.** `onclose`에서도 세대가 일치하는 종료만 "예기치 않은 종료"로 판단해 재접속하므로, 의도적 종료와 사고를 구분할 수 있음.

같은 원리로 `stopAisStream`에서는 **순서**가 중요했음 — `activeBounds`를 `null`로 만들기 *전에* 남은 배치를 flush하고 캐시를 저장해야 함. 캐시 저장이 bounds를 기준으로 필터링하기 때문임 ([aisStream.ts:737](src/store/aisStream.ts:737)).

**재접속** — 예기치 않은 종료 시 1초에서 시작해 30초 상한까지 지수 백오프하며, ±20% 지터를 넣어 동시 재접속 몰림을 방지함. 재접속 중에도 **기존 선박 목록은 유지**해 화면이 비지 않음.

---

### 4. 충돌 위험 계산이 O(n²)였던 문제

**문제** — 2초마다 모든 쌍을 검사하면 n(n−1)/2 쌍이라, **500척이면 한 번의 스캔에 약 12만 5천 쌍**이 됨. 여기에 초기 구현은 선박마다 `updateShip()`을 개별 호출했고, Zustand의 `set`은 매번 `ships` 객체 전체를 복사하므로 **500번의 전체 복사 + 500번의 구독자 알림**이 얹혔음. 2초 주기로 화면이 얼어붙었음.

**해결** — 두 가지를 바꿨음.

**(1) 질문을 다시 정의해 스캔을 O(n)으로 만들었음.** 관제 화면이 실제로 답해야 하는 질문은 "모든 배 사이의 위험"이 아니라 **"선택한 본선 기준의 위험"** 임 — ARPA가 CPA를 계산하는 방식 그대로임. 그러면 스캔은 선택 선박 대 나머지 전부, 즉 O(n)이 됨.

**(2) 단일 패스 + copy-on-write로 재작성했음** ([useShipStore.ts:181](src/store/useShipStore.ts:181)).

```ts
let nextShips = state.ships;   // 처음엔 원본 참조 그대로
for (const id of Object.keys(state.ships)) {
  // ... 위험도 계산 ...
  if (riskChanged || zoneChanged || alertsChanged) {
    if (nextShips === state.ships) {
      nextShips = { ...state.ships };   // 실제 변경이 있을 때 딱 한 번만 복사
    }
    nextShips[id] = { ...ship, risk: nextRisk, /* ... */ };
  }
}
return changed ? { ships: nextShips } : {};  // 변경 없으면 아예 알림 안 함
```

**결과** — 전체 복사 500회 → 최대 1회, 구독자 알림 500회 → 최대 1회. 변화가 없는 틱에서는 빈 객체를 반환해 **리렌더 자체가 발생하지 않음.**

---

### 4-1. 충돌 위험 분석 (CPA / TCPA) — 공식이 아니라 그 공식을 감싸는 부분이 본체

**CPA/TCPA 공식 자체는 어렵지 않음.** 상대속도 벡터로 푸는 2차식이라 [maritimeMath.ts](src/utils/maritimeMath.ts)에서 실질 15줄임. 시간을 쓴 곳은 공식이 아니라 **그 공식을 실데이터로 감쌌을 때 틀리는 지점들**이었음. AIS는 결측이 흔하고, 좌표는 미터가 아니라 각도이며, 값은 매 초 흔들림.

**(1) 좌표계 — 위경도를 그대로 빼면 22% 틀림**

위경도는 각도라서 그대로 유클리드 거리를 내면 안 됨. 위도 1도는 어디서나 약 111km지만, 경도 1도는 극으로 갈수록 짧아짐. 부산(약 35°N)에서 `cos 35° ≈ 0.819`이므로 보정 없이 계산하면 **동서 거리를 약 22% 과대평가**함.

```ts
const lngScale = latScale * Math.cos(degToRad(refLat));  // 기준 위도에서의 경도 축척
```

500m 임계값에서는 이 한 줄이 등급을 바꿈. 실제로 부산 앞바다에서 경도 0.005° 차이는 **455m**인데, 보정을 빼면 **556m**로 계산돼 danger(500m 미만)를 통째로 놓침. 이건 화면상으로는 전혀 티가 안 나는 종류의 버그임.

**(2) 수치 예외 — 0으로 나누기와 과거의 최근접점**

같은 침로 · 같은 속력으로 나란히 가는 두 배는 상대속도가 0이라 TCPA 식의 분모가 사라짐. 상대속도 제곱이 `1e-6` 미만이면 현재 거리를 그대로 CPA로 씀. 최근접점이 이미 지나간 경우(`tcpa < 0`)도 같은 경로로 떨어뜨림.

**(3) 오경보 차단 — 거리만 보면 안 됨**

거리만 보면 **이미 최근접점을 지나 멀어지는 배**도 danger로 뜸. TCPA 부호와 시간 지평(danger 6분 / warning 12분)을 함께 봐서, 지금 조치가 필요한 조우만 위험으로 판정함. 최근접점을 통과한 배는 50m 옆이라도 safe임.

오경보를 이만큼 신경 쓴 이유는 단순함 — **경보 하나가 틀리면 그 화면의 경보 전체가 무시당함.**

**(4) 없는 값을 만들어내지 않음**

침로(COG)와 방위(heading)를 **둘 다 모르는 선박은 침로를 지어내는 대신 정지 상태로 간주**함. 프록시가 AIS 센티널 값(COG 360, heading 511)을 정직하게 `null`로 정규화하는데, 여기서 0°를 기본값으로 채우면 **"진북으로 항해 중"이라는 존재하지 않는 벡터**가 만들어짐. Class-B 소형선에 흔한 케이스라 그냥 두면 허위 경보와 경보 누락이 동시에 생김.

**(5) 경보가 깜빡이지 않게 — 슈미트 트리거 + 강등 유예**

CPA가 임계값 근처에서 몇 미터씩 흔들리면 등급이 매 스캔마다 뒤집힘. 499m → danger, 501m → warning, 다시 danger. 값은 매번 정확한데 화면은 못 씀.

진입선과 해제선을 분리하고, 전이를 비대칭으로 만들어 해결했음 ([riskGrading.ts](src/utils/riskGrading.ts)).

- **진입 500m / 해제 650m** — 이미 경보 중인 선박은 넓힌 임계값으로 다시 판정함. 같은 550m가 이전 상태에 따라 다르게 판정됨
- **승격은 즉시, 강등은 20초 유예** — 강등 조건이 20초 연속 유지돼야 실제로 내려감. 중간에 다시 가까워지면 타이머가 초기화됨
- **경보 피드는 에지 트리거** — safe/warning → danger로 넘어가는 순간에만 발생하고, MMSI당 5분 디듀프를 백스톱으로 검

켜지는 건 빨라야 하고 꺼지는 건 느려야 함. 경보에서 이 두 방향은 대칭이 아님.

**(6) 검증 — 눈으로 확인할 수 없는 것을 테스트로 고정**

판정 로직 전체를 렌더링과 무관한 순수 함수로 떼어내고, **항해 조우 상황을 그대로 테스트 케이스로 옮겼음.**

| 시나리오 | 검증하는 것 |
|---|---|
| 정면 대치 (2km, 각 10m/s) | CPA 0m, TCPA 100초 |
| 직각 횡단 | 현재 1.4km 떨어져 있어도 100초 뒤 충돌 |
| 추월 (뒤에서 2배 속력) | 횡방향 이격 100m |
| 정지 표적 | 본선만 움직여도 CPA 성립 |
| 평행 항주 | 0으로 나누지 않고 유한값 |
| 이격 중 | 1km 거리인데도 safe (오경보의 주범) |
| 임계값 왕복 (495↔505m) | 10회 반복해도 등급이 한 번도 안 뒤집힘 |
| 강등 타이머 리셋 | 중간 복귀 시 20초를 새로 셈 |

Vitest 42개 통과 — `npm test`.

이렇게 분리한 이유는 **해상 수학을 눈으로 검증할 수 없기 때문**임. 화면에서 배가 그럴듯하게 움직여도 22% 틀린 거리로 경보를 내고 있을 수 있고, 그건 스크린샷으로 안 드러남. 계산부를 UI에서 떼어놓아야 "이 숫자가 맞다"를 증명할 수 있음.

---

### 5. 수백 개 마커를 끊김 없이 움직이기

**문제** — AIS 위치 보고는 배마다 수 초~수십 초 간격으로 불규칙하게 옴. 그대로 찍으면 마커가 뚝뚝 끊겨 순간이동하는 것처럼 보임. 그렇다고 React state로 매 프레임 보간하면 초당 60회 × 250개 리렌더가 발생함.

**해결** — **모든 마커가 하나의 rAF 티커를 공유**하고, React를 거치지 않고 Leaflet 레이어를 직접 조작함 ([markerAnimation.ts](src/components/map/markerAnimation.ts)).

- 티커는 전역에 단 하나, 진행 중인 보간이 없으면 자동으로 멈춤
- `layer.setLatLng()`으로 DOM을 직접 갱신 → **React 리렌더 0회**
- 500m 이상 점프(신호 유실 후 복귀)는 보간 없이 스냅 — 배가 바다를 가로질러 미끄러지는 것처럼 보이지 않게
- 줌 11 미만이거나 마커가 150개를 넘으면 보간을 끄고 스냅
- OS의 `prefers-reduced-motion` 설정을 존중 (접근성)

---

### 6. 새로고침할 때마다 빈 화면이 보이는 문제

**문제** — 소켓 연결 → 구독 → 첫 데이터 도착까지 1~2초 동안 지도가 텅 비어 있어, 앱이 고장난 것처럼 보였음.

**해결** — 2단 웜 스타트를 구현했음.

1. **클라이언트 캐시** — 마지막으로 본 선박들을 localStorage에 저장(TTL 10분, 500척)했다가, 스트림 시작 즉시 스토어에 주입 ([persistence.ts](src/store/persistence.ts)). React 이펙트 클린업은 페이지 이탈 시 실행되지 않으므로 `pagehide` 이벤트에서 최종 저장하고, 해역 전환 직후처럼 스토어가 순간적으로 빈 상태에서는 기존 캐시를 덮어쓰지 않도록 가드함
2. **서버 캐시** — 프록시가 클라이언트 접속 여부와 무관하게 주요 3개 해역(부산 · 인천 · 싱가포르 해협)을 상시 구독해 캐시를 채워둠. 신규 접속 시 **스냅샷을 100척 청크로 즉시 전송**

**결과** — 새로고침 후 지도가 비어 있는 시간이 사실상 사라졌음.

---

### 7. 정보가 없는 배를 정직하게 다루기

**문제** — AIS는 **위치 메시지(수 초 간격)** 와 **정적 메시지(5~6분 간격)** 가 분리되어 있음. 방금 화면에 들어온 배는 위치는 있는데 이름 · IMO · 목적지가 없음. 게다가 정적 정보가 위치보다 **먼저** 도착하는 경우도 있음.

**해결**

- 정적 정보가 먼저 오면 `pendingStaticByMmsi`에 최대 300건까지 보류했다가, 위치가 도착하는 순간 병합함 ([aisStream.ts:350](src/store/aisStream.ts:350))
- 병합 규칙을 명확히 분리했음 — **정적 필드는 정보를 "추가"만 하고 `null`로 지우지 않음.** 보고 종류에 따라 필드가 정상적으로 누락되기 때문임. 반면 침로 · 방위 같은 동적 필드는 명시적 `null`이 오면 덮어씀 (낡은 값이 화면에 남지 않도록)
- 위치 없는 정적 정보만으로는 **선박 객체를 만들지 않음** — 좌표 (0, 0)에 유령 선박이 뜨는 것을 방지

**이게 중요한 이유** — 데이터가 없을 때 그럴듯한 값으로 채우는 대신 "미상"으로 두는 것이, 관제 시스템에서는 훨씬 안전한 설계라고 판단했음.

---

## 그 외 구현

- **운영 모드** — Fleet / Safety / Marina 세 가지 모드 전환. Analytics는 전체 통계가 아니라 **세션 집계**임을 화면에 명시함
- **다국어** — i18next로 한국어 · 영어 · 일본어 지원, 브라우저 언어 자동 감지 — 번역 테이블 추가만으로 언어 확장 가능
- **선박 검색** — 로컬 스토어 + 프록시 `/search`(서버 캐시 5,000척 대상)를 병합. 요청마다 `AbortController`로 이전 요청 취소
- **연결 상태 패널** — Settings에서 프록시 `/health`를 15초마다 폴링해 상류 연결 상태 · 업타임 · 캐시 크기 · 적용 중인 제한값을 **가공 없이 그대로** 표시
- **지오펜싱** — 제한 수역 진입 시 엣지 트리거 알림 (진입 순간에만 1회)
- **알림 피드** — MMSI당 5분 중복 제거, 최대 100건 유지
- **3D 뷰** — Three.js로 선택한 선박을 3D 렌더링. 실제로 수신되는 방위(heading)와 속도만 Lerp 보간으로 반영
- **Analytics** — 현재 세션에서 실제로 추적 중인 AIS 데이터만 클라이언트에서 집계 (선종 분포, 속도 히스토그램, 항행 상태 분포)
- **커스텀 마커** — 선종별 색상 · 방위 회전 SVG `DivIcon`. 아이콘을 캐시해 재생성 비용 제거

---

## 한계와 배운 점

**정직하게 밝히는 한계 —**

- **AIS 규격에는 pitch/roll 정보가 없음.** 초기에는 3D 뷰에 선체 동요를 넣으려 했지만, 실제 데이터로는 불가능하다는 것을 확인하고 제거했음. 현재 3D 뷰는 실제로 수신되는 방위 · 속도만 반영함.
- 같은 이유로 **연료 소모량 · CO2 배출량 추정 기능도 제거했음.** AIS로는 계산할 수 없는 값이고, 근거 없는 숫자를 대시보드에 띄우는 것은 관제 도구로서 위험하다고 판단했음.
- Analytics는 "전체 통계"가 아니라 **"현재 세션에서 추적한 선박의 집계"** 임. 화면에도 그렇게 표기했음.
- 서버 캐시는 인메모리 `Map`이라 프록시 재시작 시 사라짐. 다중 인스턴스로 확장하려면 Redis 같은 외부 저장소가 필요함.
- **추측항법(dead reckoning) 보간 미적용.** AIS 갱신 주기는 정박 3분 · 저속 10초 · 고속 2초로 제각각이라, 정박 중인 선박과 통신이 끊긴 선박이 화면에서 똑같아 보임.
- **히스테리시스 상수(해제선 1.3배, 강등 유예 20초)는 실측이 아니라 판단으로 정한 값임.** 플래핑을 없애는 것과 위험이 지나간 뒤 경보가 늦게 꺼지는 것 사이의 절충이며, 실제 관제 운용 로그 없이는 최적값을 알 수 없음.
- **CPA 판정이 점(point) 기준임.** 실제 위험은 선박 길이 · 폭을 고려한 선체 간 거리로 봐야 함.
- **COLREG 조우 상황 미분류.** 마주침 · 추월 · 횡단에 따라 회피 의무가 다른데 현재는 거리와 시간만 봄.
- **시계열 저장소 없음.** 장기 항적 재생 · 통계에는 별도 저장소가 필요함.
- **성능 수치 미측정.** 최적화 전후를 FPS · 프레임 타임 · 초당 처리 메시지 수로 남기는 것이 남아 있음.

**배운 점 —**

가장 크게 배운 것은 **"실시간"이 성능 문제가 아니라 설계 문제**라는 점임. 처음에는 `React.memo`와 `useMemo`를 더 붙이면 해결될 줄 알았지만, 실제 해법은 **데이터가 화면에 도달하기 전에 어느 계층에서 얼마나 줄일 것인가**를 결정하는 일이었음. 프록시 서버에서 초당 180건으로 제한하는 한 줄이, 프론트엔드의 어떤 메모이제이션보다 효과적이었음.

두 번째는 **O(n²)는 계산이 아니라 정의로 푼다**는 점임. 2초마다 모든 쌍을 검사하면 500척에 약 12만 5천 쌍이 됨. 그런데 관제 화면이 답해야 하는 질문은 "모든 배 사이의 위험"이 아니라 **"선택한 본선 기준의 위험"** 이므로, 스캔을 O(n)으로 재정의하는 것만으로 문제 자체가 사라졌음.

세 번째는 **없는 데이터를 만들어내지 않는 것의 가치**임. 목업으로 채우면 화면은 화려해지지만, 그 순간 이 프로젝트는 "실시간 관제 시스템"이 아니라 "관제 시스템처럼 보이는 화면"이 됨. 기능을 덜어내는 결정이 오히려 프로젝트의 신뢰도를 높였다고 생각함.

네 번째는 **공식을 아는 것과 그 공식을 믿을 수 있게 만드는 것은 다른 일**이라는 점임. CPA/TCPA는 검색하면 나오는 2차식이고 구현은 15줄이면 끝남. 정작 시간이 든 곳은 좌표계 보정, 0으로 나누기, 결측값 처리, 임계값 근처의 플래핑, 그리고 "이 숫자가 맞다"를 증명하기 위한 순수 함수 분리와 테스트였음. **알고리즘을 쓸 줄 아는 것보다, 그 알고리즘이 실데이터에서 어디서 깨지는지 알고 막아두는 쪽이 실제로 어려웠음.**

---

## 실행 방법

```bash
npm install
```

`.env` 파일에 AISStream API 키를 설정함 (키는 [aisstream.io](https://aisstream.io)에서 무료 발급):

```
AISSTREAM_API_KEY=your_api_key_here
# 포트는 미지정 시 프론트·프록시 모두 8080을 씀. 8080이 사용 중이면
# PROXY_PORT와 VITE_PROXY_PORT를 같은 빈 포트로 함께 지정할 것 (예: 8081)
# PROXY_PORT=8080
```

프론트엔드와 프록시 서버를 함께 실행함:

```bash
npm run dev
```

---

## 디렉터리 구조

```text
seatrace/
├── server/
│   └── proxy.js            # AIS 프록시 (키 은닉·정규화·캐시·레이트리밋)
├── src/
│   ├── store/
│   │   ├── aisStream.ts    # WebSocket 클라이언트·배치·재접속·구독 관리
│   │   ├── useShipStore.ts # Zustand 스토어 + CPA/지오펜스 위험도 계산
│   │   ├── shipTypes.ts    # 도메인 타입 정의
│   │   ├── config.ts       # 튜닝 상수 (한 곳에 모아 조정 가능)
│   │   ├── persistence.ts  # localStorage 웜 캐시·설정·함대
│   │   └── riskPass.test.ts # 지오펜스 에지·등급 이력 수명 테스트
│   ├── components/
│   │   ├── map/            # Leaflet 지도·마커·클러스터·rAF 보간
│   │   ├── 3d/             # Three.js 3D 뷰
│   │   ├── dashboard/      # StatsBar, Alerts, ModeSwitcher
│   │   └── layout/         # AppLayout, Header, Sidebar
│   ├── hooks/
│   │   └── useShipSnapshot.ts  # 렌더 스로틀링 훅
│   ├── utils/
│   │   ├── maritimeMath.ts      # CPA/TCPA, 좌표 투영, 속도 벡터
│   │   ├── riskGrading.ts       # 위험 등급 판정 (슈미트 트리거 + 강등 유예)
│   │   ├── maritimeMath.test.ts # 조우 상황 골든 케이스 (Vitest)
│   │   ├── riskGrading.test.ts  # 플래핑 · 비대칭 전이 테스트
│   │   ├── alertText.ts         # 알림 번역 · 경과 시간 · 심각도 공용 포맷
│   │   └── aisTypes.ts          # AIS 선종 코드 분류
│   └── pages/              # Dashboard, LiveMap, FleetStatus, Analytics, Settings
└── .env                    # AISSTREAM_API_KEY (git 추적 제외)
```
